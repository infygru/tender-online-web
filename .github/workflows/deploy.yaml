name: Deploy to VPS
on:
  push:
    branches: prod

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'yarn'

      - name: Dependency Install
        run: yarn install --frozen-lockfile

      - name: Create .env.production file
        run: |
          echo "NEXT_PUBLIC_Google_Oauth_ClientID=${{ secrets.NEXT_PUBLIC_GOOGLE_OAUTH_CLIENTID }}" >> .env.production
          echo "NEXT_PUBLIC_API_ENPOINT=${{ secrets.NEXT_PUBLIC_API_ENDPOINT }}" >> .env.production
          echo "NEXT_PUBLIC_RAZOR_API_KEY=${{ secrets.NEXT_PUBLIC_RAZOR_API_KEY }}" >> .env.production
          echo "NEXT_PUBLIC_RAZOR_PLAN_ID_1=${{ secrets.NEXT_PUBLIC_RAZOR_PLAN_ID_1 }}" >> .env.production
          echo "NEXT_PUBLIC_RAZOR_PLAN_ID_2=${{ secrets.NEXT_PUBLIC_RAZOR_PLAN_ID_2 }}" >> .env.production
          echo "NEXT_PUBLIC_RAZOR_PLAN_ID_3=${{ secrets.NEXT_PUBLIC_RAZOR_PLAN_ID_3 }}" >> .env.production

      - name: Build
        run: yarn build

      - name: SSH setup
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Add host as known host
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to VPS
        run: |
          rsync -avz --delete -e "ssh -p ${{ secrets.SSH_PORT }}" \
            ./ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:~/tender-frontend/ \
            --exclude=".git" \
            --exclude=".github" \
            --exclude="node_modules"

      - name: Connect to server and start service
        run: |
          ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
            cd ~/tender-frontend
            yarn install --frozen-lockfile
            yarn build
            systemctl restart tender-frontend
          EOF
